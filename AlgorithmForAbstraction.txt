1) Load the template xml
2) Create a dictionary dictKeyValuePairs of [key="key" from xml, value = keywords from xml]
3) Load the transcript.txt
4) Create abstract.txt
5) Write a function : 
	GetAbstractHeader(line)
			extract keywords using rake script
			return the entry with most matching keywords from dictKeyValuePairs

	GetAbstractValue()		
			This computes the value to be placed for the abstract header

6) For every line in transcript.txt
	Is agent line ?
		Yes 
			lastAgentLine = line			
			GetAbstractHeader() or GetAbstractValue() to be called 
			
  	Is customer line 		
		Yes 
			is continutaion ?			
				customerLine += line
			else 
				customerLine = line			
			was lastAgentLine == ynquestion
				extract sentiment using nltk sentiment analysis, paste answer (Yes/No) to abstract
			GetAbstractHeader() or GetAbstractValue() to be called 										
			
